import React from 'react';


const Member = (props) => {
    return (
        <div>
        <h3>{propsn.name}</h3>
        <p>{props.age}</p>
        <p>{props.height}</p>
        </div>
    )
}

export default Member;